diff --git a/node_modules/@capgo/capacitor-nfc/ios/Sources/NfcPlugin/NfcPlugin.swift b/node_modules/@capgo/capacitor-nfc/ios/Sources/NfcPlugin/NfcPlugin.swift
index 6d0b570..d2cf0e2 100644
--- a/node_modules/@capgo/capacitor-nfc/ios/Sources/NfcPlugin/NfcPlugin.swift
+++ b/node_modules/@capgo/capacitor-nfc/ios/Sources/NfcPlugin/NfcPlugin.swift
@@ -281,7 +281,10 @@ public class NfcPlugin: CAPPlugin, CAPBridgedPlugin {
 
     private func notify(event: [String: Any]) {
         DispatchQueue.main.async {
-            self.notifyListeners("nfcEvent", data: event, retainUntilConsumed: true)
+            // PATCHED (polyx): retainUntilConsumed:false so a stale NFC event from a
+            // previous session is NOT replayed onto the next scan's listener (which
+            // caused a phantom "read" of the last-created wallet on a blank tag).
+            self.notifyListeners("nfcEvent", data: event, retainUntilConsumed: false)
             guard let type = event["type"] as? String else {
                 return
             }
@@ -419,13 +422,12 @@ extension NfcPlugin: NFCNDEFReaderSessionDelegate {
                 }
 
                 tag.readNDEF { message, readError in
-                    if let readError {
-                        session.invalidate(errorMessage: "Failed to read NDEF message: \(readError.localizedDescription)")
-                        return
-                    }
-
+                    // PATCHED (polyx): a blank/unwritten sticker returns a read
+                    // error, but for WRITING that's fine — the tag is still there.
+                    // Store the tag and emit anyway (nil message on read error) so
+                    // write() can run on brand-new blank stickers.
                     self.currentTag = tag
-                    let event = self.buildEvent(tag: tag, status: status, capacity: capacity, message: message)
+                    let event = self.buildEvent(tag: tag, status: status, capacity: capacity, message: readError == nil ? message : nil)
                     self.notify(event: event)
                 }
             }
